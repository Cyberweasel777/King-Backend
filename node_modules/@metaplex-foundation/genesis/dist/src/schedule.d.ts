import { ConditionArgs, LinearBpsSchedule, LinearBpsScheduleArgs, LinearBpsScheduleV2, LinearBpsScheduleV2Args } from './generated';
export interface SchedulePoint {
    timeBps: number | bigint;
    bps: number | bigint;
}
export declare const BPS_DIVISOR = 10000n;
export declare const MAX_TIME_BPS = 10000n;
export declare const MIN_TIME_BPS = 0n;
export declare function createLinearBpsSchedule(point1: SchedulePoint, point2: SchedulePoint, args: Omit<LinearBpsScheduleArgs, 'slopeBps' | 'interceptBps'>): LinearBpsSchedule;
export declare function getBpsFromSchedule(schedule: LinearBpsSchedule, timeBps: number | bigint): bigint;
export declare function getBpsFromScheduleTimestamps(schedule: LinearBpsSchedule, { now, start, end, }: {
    now: number | bigint;
    start: number | bigint;
    end: number | bigint;
}): bigint;
/**
 * Creates a LinearBpsScheduleV2 from two schedule points and a condition + duration.
 *
 * @param point1 - First point on the schedule line (timeBps, bps)
 * @param point2 - Second point on the schedule line (timeBps, bps)
 * @param args - startCondition, duration, and maxBps
 */
export declare function createLinearBpsScheduleV2(point1: SchedulePoint, point2: SchedulePoint, args: {
    startCondition: ConditionArgs;
    duration: number | bigint;
    maxBps: number | bigint;
}): LinearBpsScheduleV2Args;
/**
 * Evaluate a V2 schedule at a given timestamp.
 *
 * Reads `startCondition.triggeredTimestamp` as the start time.
 * Returns 0 if the condition hasn't triggered yet or if `now` is outside the schedule window.
 */
export declare function getBpsFromScheduleV2(schedule: LinearBpsScheduleV2, now: number | bigint): bigint;
